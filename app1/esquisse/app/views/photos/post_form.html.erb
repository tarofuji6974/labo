<div class="main posts-new">
  <div class="container">
    <h3>Photo</h3>
      <div class="form">
        <div class="form-body">
          <% @photo.errors.full_messages.each do |message| %>
            <div class="form-error">
              <%= message %>
            </div>
          <% end %>
          <%= form_tag("/create",{multipart: true}) do %>
            <p>Photo</p>
            <input type="file" name = "image_name">
            <p>Content</p>
            <textarea name="content"><%=@photo.content %></textarea>
            <input type="submit" value="Post" class="waves-effect waves-light btn">
          <% end %>
          <br>
          <%= button_to "Back", {controller: 'photos', action: 'index'}, {method: :get,class: "waves-effect waves-light btn"} %>
        </div>
      </div>
  </div>
</div>